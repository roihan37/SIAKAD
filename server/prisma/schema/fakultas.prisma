model Fakultas {
  id        Int      @id @default(autoincrement())
  kode      String   @unique
  name      String
  createdAt DateTime @default(now())
  updatedAt DateTime @updatedAt
  prodi     Prodi[]
}
